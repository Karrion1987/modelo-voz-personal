services:
  - type: web
    name: modelo-voz-personal
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: PORT
        value: 8000
      - key: HOST
        value: 0.0.0.0
